{
  "name": "ai-enhancement-app",
  "version": "1.0.0",
  "private": true,
  "description": "Turn a share-sheet tap on a social post into an analyzed, specified, and (on your approval) autonomously implemented improvement to your AI setup.",
  "license": "MIT",
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/web",
    "packages/desktop-bridge"
  ],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "npm run build --workspace @aiapp/shared && npm run build --workspace @aiapp/server && npm run build --workspace @aiapp/web",
    "build:server": "npm run build --workspace @aiapp/shared && npm run build --workspace @aiapp/server",
    "dev": "concurrently -n server,web -c cyan,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm run dev --workspace @aiapp/server",
    "dev:web": "npm run dev --workspace @aiapp/web",
    "start": "npm run start --workspace @aiapp/server",
    "test": "npm run test --workspace @aiapp/server",
    "typecheck": "tsc -b packages/shared packages/server",
    "bridge": "npm run start --workspace @aiapp/desktop-bridge"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  }
}
